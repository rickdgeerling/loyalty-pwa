import Alpine from 'alpinejs';
import bwipjs from 'bwip-js';
import { BrowserMultiFormatReader } from '@zxing/library';

// Stub — will be populated in later steps
// bwip-js and @zxing/library imports above validate esbuild ESM bundling
void bwipjs;
void BrowserMultiFormatReader;
Alpine.start();
